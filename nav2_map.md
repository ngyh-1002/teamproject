
---

## 📋 프로젝트 요약 및 변경 사항

| 구분 | 내용 | 상세 설명/변경 전후 비교 |
| :--- | :--- | :--- |
| **목표 환경** | 우분투 22.04, ROS 2 Humble | 호텔 배달 로봇을 이용해 특정 방으로 음식을 배달하는 프로젝트 |
| **1단계: 방 좌표 획득** | Rviz 사용 및 `/amcl_pose` 데이터 획득 | Rviz로 목표 위치 이동 후 `/amcl_pose`에서 위치($x, y$)와 방향($x, y, z, w$ 쿼터니언) 추출 |
| **2단계: 좌표 YAML 파일 작성** | 위치: $x, y$ (z는 제외), 방향: $\theta$ (오일러 각 변환) | 사람이 인식하기 쉽도록 $\theta$ 값만 **오일러 각**으로 변환하여 저장 |
| **3단계: 내비게이션 노드에서의 변환** | 쿼터니언 재변환 | YAML의 $\theta$를 사용하고, $x, y, z$ 회전 축 값은 $0$, $z$ 축에 $\theta$를 적용하여 **쿼터니언**으로 변환 후 목표 전송 |
| **문제 발생** | 로봇이 경로를 제대로 찾지 못함 | 주로 좁은 길목에서 경로 탐색 실패 또는 비효율적 경로 생성 문제로 추정 |
| **해결책 1: 로봇 크기 설정 변경** | `robot_radius` $\to$ `footprint` | **변경 전:** `robot_radius: 0.22` (원형 가정) |
| | | **변경 후:** **`footprint: "[[0.31, 0.29], [0.31, -0.29], [-0.31, -0.29], [-0.31, 0.29]]"`** (직사각형 모델) |
| **해결책 2: 팽창 반경 (Inflation Radius) 조정** | 길목 좁은 환경에 맞게 축소 | **변경 전:** `inflation_radius: 0.70` |
| | | **변경 후:** **`inflation_radius: 0.35`** |

---

## 📝 상세 논리적 정리

### 1. 📌 목표 좌표 설정 및 데이터 처리 방식

프로젝트는 **호텔 배달 로봇**의 자율 주행을 위해 특정 방의 목표 좌표를 설정하는 과정으로 시작했습니다.

* **좌표 획득:** Rviz 툴을 사용하여 로봇을 원하는 목표 방 위치로 직접 이동시킨 후, ROS 2의 **Adaptive Monte Carlo Localization (AMCL)** 노드에서 출력되는 `/amcl_pose` 토픽의 데이터를 활용했습니다. 이 토픽은 로봇의 현재 위치($x, y, z$)와 방향($x, y, z, w$ **쿼터니언**)을 제공합니다.
* **YAML 파일 작성:** 획득한 데이터 중 위치는 $x$와 $y$ 값만 사용했으며, 방향(Orientation)은 사람이 직관적으로 인식하고 수정하기 쉽도록 쿼터니언에서 $z$ 축 회전에 대한 **오일러 각 ($\theta$)**으로 변환하여 YAML 파일에 저장했습니다.
* **내비게이션 노드 처리:** 실제 로봇에게 목표를 전송하는 내비게이션 노드 코드에서는 YAML 파일의 $\theta$ 값을 다시 ROS 2가 요구하는 **쿼터니언** 형태로 변환했습니다. 이때, $x, y, z$ 회전 값 중 **$z$ 축 회전값에만 $\theta$**를 대입하고, 나머지 $x$ 축과 $y$ 축 회전값은 $0$으로 설정하여 목표 포즈(Goal Pose) 메시지를 구성했습니다. 이 과정은 2D 평면에서의 이동(Yaw 회전만 고려)을 가정하는 일반적인 **Nav2** 시스템의 목표 설정 방식에 부합합니다.

### 2. 🚧 경로 탐색 문제 발생 및 원인 분석

초기에 로봇이 목표 방까지의 **경로를 제대로 찾지 못하는 문제**가 발생했습니다. 이 문제는 특히 호텔 복도와 같이 **좁은 길목**을 지나야 할 때 두드러졌을 가능성이 높습니다. 경로 계획 실패의 주요 원인은 **Nav2**의 Costmap 설정이 로봇의 실제 크기와 환경적 제약을 적절히 반영하지 못했기 때문으로 분석됩니다.

### 3. ✅ 로봇 크기 및 비용 지도(Costmap) 설정 개선

경로 탐색 문제를 해결하기 위해 **`nav2_param.yaml`** 파일 내의 **`local_costmap`** 및 **`global_costmap`** 설정 블록에서 두 가지 핵심 매개변수를 수정했습니다.

#### A. 로봇 크기 정의: `robot_radius` $\to$ `footprint`

* **변경 논리:** 이전 설정인 `robot_radius: 0.22`는 로봇을 **반경 $0.22\text{ m}$의 원형**으로 가정하여 충돌을 회피합니다. 그러나 실제 로봇인 '스카우트 미니'는 길이 $0.62\text{ m}$, 폭 $0.58\text{ m}$의 **직사각형** 형태입니다. 원형 모델은 실제 로봇보다 작은 공간을 차지한다고 오인하게 하거나, 직사각형 형태를 가진 로봇의 코너 부분이 충돌을 일으킬 위험을 간과하게 합니다.
* **개선:** 로봇의 실제 크기를 정확히 반영하기 위해 `robot_radius` 설정을 제거하고 **`footprint`** 매개변수를 사용했습니다. 로봇 크기($0.62\text{ m} \times 0.58\text{ m}$)의 절반인 $0.31\text{ m} \times 0.29\text{ m}$를 이용하여 직사각형의 네 꼭짓점 좌표를 정의했습니다.
    $$
    \text{footprint}: \text{"}[[0.31, 0.29], [0.31, -0.29], [-0.31, -0.29], [-0.31, 0.29]]\text{"}
    $$
    이 변경을 통해 Nav2는 로봇의 실제 직사각형 형태를 기반으로 더욱 **정확하고 안전한 경로**를 계획할 수 있게 되었습니다.

#### B. 팽창 반경(Inflation Radius) 조정

* **변경 논리:** **`inflation_radius`**는 장애물 주변에 비용(Cost)을 부풀려서 로봇이 장애물에 너무 가깝게 접근하는 것을 방지하는 안전 거리입니다. 원래 $0.70\text{ m}$로 설정되어 있었는데, 이는 로봇이 장애물에서 **최소 $0.70\text{ m}$** 떨어진 경로를 선호한다는 의미입니다. 호텔 복도와 같은 **좁은 길목** 환경에서 이 값은 너무 커서 로봇이 장애물에 지나치게 가깝게 가지 못하게 막아 유효한 경로를 찾지 못하거나, 아예 경로가 존재하지 않는다고 판단하게 만들 수 있습니다.
* **개선:** 좁은 환경에서의 경로 탐색 성공률을 높이기 위해 **`inflation_radius`를 $0.70\text{ m}$에서 $0.35\text{ m}$로** 대폭 줄였습니다. 이는 로봇이 장애물에 더 가깝게 (하지만 여전히 안전하게) 접근할 수 있도록 허용하여, 좁은 복도에서도 효과적으로 경로를 생성하고 목표에 도달할 수 있게 하는 핵심적인 조치였습니다.

## 요약
## 📝 프로젝트 단계별 요약 (단답형 1줄 요약)

### 1. 초기 설정 및 변환

**Rviz 이동 후 `/amcl_pose`에서 좌표를 얻고, 이를 사람이 읽기 쉬운 오일러각($\theta$)으로 YAML에 저장한 뒤, 내비게이션 노드에서 $\theta$를 $z$축 쿼터니언으로 재변환하여 목표를 전송했습니다.**

---

### 2. 문제 해결 및 환경 조정

**경로 탐색 문제를 해결하기 위해, `robot_radius: 0.22` 대신 실제 크기에 맞는 `footprint`를 사용하고, 좁은 길목에 맞게 `inflation_radius`를 $0.70\text{ m}$에서 $0.35\text{ m}$로 줄였습니다.**
